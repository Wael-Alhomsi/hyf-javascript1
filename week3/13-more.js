'use strict';
const bar = 42;
const result = typeof typeof bar;
console.log(result);
console.log('The value of "result" would be a "string" because types of variables are expressed in the form of strings.');
